#---
fillin:
  enabled: false  # Enable the form repopulation

fields:
  hotels_reservation{reserved_from}:
    required: { msg: Required }
  hotels_reservation{reserved_to}:
    required: { msg: Required }